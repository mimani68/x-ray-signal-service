export enum MODE {
  DEVELOP = 'DEV',
  PRODUCTION = 'PROD',
  TEST = 'TEST',
}

export enum LOG_LEVEL {
  INFO = 'info',
  WARNING = 'warning',
  ERROR = 'error',
}

export enum PROMISE_ALL_STATUS {
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected',
}
